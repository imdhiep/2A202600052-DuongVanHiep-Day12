# Screenshots

Add real screenshots here after deploying the service:

- `redis.jpg`            # already captured
- `dashboard.png`        # Railway app dashboard
- `running.png`          # service running / health check
- `test.png`             # authenticated API test result

These files are referenced from `DEPLOYMENT.md`.
